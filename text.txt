imp import uberon UBERON:13234
adds row for UBERON:123432 to file with action "import"

find ID & determine whether it's a class/relation
block --> ignore
imp ignore uberon UBERON:12334

merge term and termlist into a single function to check if input is a file, IRI, CURIE, or term label, probably in that order

imp remove uberon UBERON:13443
(drop --> remove)

imp import uberon UBERON:31423 -p "material entity"

imp import uberon UBERON:02343 -u

can use full path in place of uberon

global option (--config) that will apply to all subcommands
global --> local --> environment variables --> command line options

look at argparse documentation more carefully

avoid config files if possible
